{
  "name": "api",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --local --port=8787",
    "deploy": "wrangler deploy --minify",
    "check": "tsc --noEmit",
    "lint": "echo \"no lint configured\""
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "wrangler": "^4.33.1"
  }
}
